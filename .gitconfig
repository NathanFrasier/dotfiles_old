[alias]
  # basic
  co = checkout
  st = status
  br = branch

  # additional
  unstage = reset HEAD
  discard = checkout --

  # diffs
  ds = diff --staged
  wd = diff --word-diff --ignore-all-space
  wds = diff --word-diff --ignore-all-space --staged

  # logs
  g = log --graph --decorate --pretty=format:'%C(yellow)%h%C(bold white) - %s%C(auto)%d %C(dim white)- %an%C(reset)'

  #status
  sts = status -s
  stl = status
